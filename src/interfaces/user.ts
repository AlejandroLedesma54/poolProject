export interface User {
    id: number;
    name: string;
    email: string;
  }

//type se usa para interfaces pequenas, solamente un nombre, email etc
// interface: yo quiero referirme a una entidad o una estructura muy grande